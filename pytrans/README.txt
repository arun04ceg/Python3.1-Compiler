
1) Name : Arun Baskar(u0811338) & Arvind Haran(u0809850).

2) We rely on the pylex and pyparse executables present in the server.

3) The only file we changed in the stub code given by the professor is the "pytrans.rkt" file.

Files turned in:

1) pytrans.rkt 
2) hir-header.rkt
3) README.txt 
4) Makefile

Make options:

1) make ->  compile the program.
2) make run ->  accept input on STDIN and send output to STDOUT.
3) make trans -> accepts output of project 2 on STDIN and send output to STDOUT.
4) make test -> run the tests on python files available in "tests/" folder.

