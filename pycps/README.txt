
1) Name : Arun Baskar(u0811338) & Arvind Haran(u0809850).

2) We rely on the pylex pyparse and pytrans executables present in the server.

3) The only files we changed in the stub code given by the professor are "pydesugar.rkt" and "pycps.rkt" files.

Files turned in:

1) pydesugar.rkt
2) pycps.rkt
3) lir-header.rkt
4) cps-header.rkt
5) README.txt 
6) Makefile

Make options:

1) make ->  compile the program.
2) make run ->  accepts input on STDIN and send output to STDOUT.
3) make cps -> accepts output of project 3 on STDIN and send output to STDOUT.
4) make test -> run the tests on python files available in "tests/" folder.
