(program
 (define break (void))
 (define return (void))
 (define continue (void))
 (define $current-handler (void))
 (begin
   (call/ec
    (lambda (break)
      ((lambda (loop)
         (begin
           (set! loop
             (lambda ()
               (if #f
                 (begin
                   (call/ec
                    (lambda (continue) ((lambda () (begin (py-print 10))))))
                   (loop))
                 (void))))
           (loop)
           ((lambda () (begin (py-print 20))))))
       (void))))))
