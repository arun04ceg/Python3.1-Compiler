(program
 (define break (void))
 (define return (void))
 (define continue (void))
 (define $current-handler (void))
 (define g$ii (void))
 (define g$fncs (void))
 (define g$identity2 (void))
 (define g$identity (void))
 (begin
   (set! g$identity
     (lambda (x) (call/ec (lambda (return) ((lambda () (begin (return x))))))))
   (set! g$identity2
     (lambda (x)
       (call/ec (lambda (return) ((lambda () (begin (return (* 2 x)))))))))
   (set! g$fncs (py-list* g$identity g$identity2))
   (call/ec
    (lambda (break)
      ((lambda ($seq16 $loop17)
         (begin
           (begin
             (if (set? $seq16)
               (for-set $seq16 $loop17)
               (if (tuple? $seq16)
                 (for-tuple $seq16 $loop17)
                 (if (py-list? $seq16)
                   (for-py-list $seq16 $loop17)
                   (if (dict? $seq16) (for-dict $seq16 $loop17) (void)))))
             (void))))
       (py-list* 1 2 3 4 5)
       (lambda (i16)
         (call/ec
          (lambda (continue)
            (begin
              (set! g$ii i16)
              ((lambda ()
                 (begin
                   (py-print (g$identity g$ii))
                   (py-print (g$identity2 g$ii))))))))))))))
