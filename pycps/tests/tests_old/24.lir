(program
 (define break (void))
 (define return (void))
 (define continue (void))
 (define $current-handler (void))
 (define g$x (void))
 (begin
   (set! g$x 0)
   (if ((lambda (cv16)
          (begin
            (if (< g$x cv16)
              ((lambda (cv17)
                 (begin
                   (if (< cv16 cv17)
                     ((lambda (cv18)
                        (begin
                          (if (< cv17 cv18)
                            ((lambda (cv19)
                               (begin (if (< cv18 cv19) (< cv19 5) #f)))
                             4)
                            #f)))
                      3)
                     #f)))
               2)
              #f)))
        1)
     ((lambda () (begin (py-print "Good!"))))
     (if (< g$x 6)
       ((lambda () (begin (py-print "WTF?"))))
       ((lambda () (begin (py-print "Bad!"))))))))
