(program
 (define break (void))
 (define return (void))
 (define continue (void))
 (define $current-handler (void))
 (define g$x (void))
 (begin
   (set! g$x #t)
   (call/ec
    (lambda (break)
      ((lambda (loop)
         (begin
           (set! loop
             (lambda ()
               (if g$x
                 (begin
                   (call/ec
                    (lambda (continue)
                      ((lambda () (begin (set! g$x #f) (continue (void)))))))
                   (loop))
                 (void))))
           (loop)
           (void)))
       (void))))
   (py-print 42)))
